# ROADMAP

`claude-toolkit-console`(ctk)의 작업 이력과 남은 일. **에픽 → 작업 단위**로 나누고 완료 여부를
표시한다.

> 이 문서의 목적은 **컨텍스트가 없는 상태에서 이어받을 수 있게** 하는 것이다. 왜 그렇게
> 결정했는지가 필요하면 `CLAUDE.md`(판단 기준)와 `docs/harness-facts.md`(실측 사실)를 본다.
> 상세 계획서는 `.omc/plans/ctk-v1-consensus-plan.md`에 있다.

| | |
|---|---|
| **현재 버전** | v1 완료 (2026-08-23) · 결함 수정 (~2026-08-26) · **B1 완료 (2026-08-27)** |
| **머지된 PR** | 33건 |
| **작업 단위** | 완료 102 · 부분 0 · 미착수 9 |
| **테스트** | 1309 passed + 5 skipped(유료 게이트) |
| **알려진 로컬 실패** | `move-rollback.test.ts` 1건 — 살아 있는 Claude Code 세션이 `.claude.json`의 `pluginUsage`를 갱신해 actuator 의미 diff에 걸린다. CI(세션 없음)는 통과 |
| **표준 검증** | `pnpm verify` — 7개 게이트(B1이 `gate:asset-kind` 추가). ⚠️ `&&` 체인이라 **알려진 로컬 실패가 뒤 게이트를 막는다** — 로컬에서는 개별로 돌린다 |

범례: `[x]` 완료 · `[ ]` 미착수 · `[~]` 부분 완료(범위가 좁혀졌거나 일부 축이 미측정)

---

## 목차

- [에픽 요약](#에픽-요약)
- [E0 — 하네스 전제 실증](#e0--하네스-전제-실증)
- [E1 — 기반: 스키마·계층·CI](#e1--기반-스키마계층ci)
- [E2 — 조회: 스캔과 카탈로그](#e2--조회-스캔과-카탈로그)
- [E3 — 쓰기: 스코프 이관과 롤백](#e3--쓰기-스코프-이관과-롤백)
- [E4 — 사용량 측정](#e4--사용량-측정)
- [E5 — 문서 자동 생성](#e5--문서-자동-생성)
- [E6 — 에이전트 진입점](#e6--에이전트-진입점)
- [E7 — 웹 콘솔](#e7--웹-콘솔)
- [E8 — 보안 심사 대응](#e8--보안-심사-대응)
- [E9 — 릴리스 게이트](#e9--릴리스-게이트)
- [E10 — 사용자 문서](#e10--사용자-문서)
- [남은 작업](#남은-작업)
  - [백로그 — 사용성 개선](#백로그--사용성-개선)
- [비범위](#비범위)

---

## 에픽 요약

| 에픽 | 내용 | 상태 | PR |
|---|---|---|---|
| **E0** | 하네스 전제 실증 (스파이크) | ✅ | #1 |
| **E1** | 스키마·계층 경계·CI | ✅ | #1, #4 |
| **E2** | `scan` · 카탈로그 · 드리프트 | ✅ | #1 |
| **E3** | `move` · `rollback` (actuator) | ✅ | #2 |
| **E4** | `measure` · `usage` | ✅ | #3, #14 |
| **E5** | `gen` — LLM 문서 생성 | ✅ | #3, #18~#28 |
| **E6** | `toolkit-search` 스킬 | ✅ | #4, #11 |
| **E7** | `ctk web` 조회·액션 | ✅ | #5~#9, #14 |
| **E8** | 보안 심사 High→Low 전건 | ✅ | #2, #6~#10 |
| **E9** | 릴리스 게이트 4종 | ✅ | #11~#13 |
| **E10** | README·사용 가이드 · ROADMAP | ✅ | #14~#16 |
| **E11** | **B1 — 번들 하위 툴 편입 + 계층 표시** | ✅ | #29~#33 |

**v1 요구사항 8개 전건 충족.** 상세는 각 에픽 참조.

---

## E0 — 하네스 전제 실증

> **왜 먼저인가**: 이 계획의 모든 설계가 "Claude Code 하네스가 이렇게 동작한다"는 전제 위에
> 서 있다. 전제가 틀리면 계획 전체가 틀린 바닥에 선다. 그래서 착수 전에 **실제로 실행해**
> 확인했다.

- [x] **E0.1** 설정 파일 구조 실측 — 설치 스코프는 `installed_plugins.json`, `settings.json`의
      `enabledPlugins`는 스코프를 담지 않음
- [x] **E0.2** `claude plugin list --json` 스키마 확정 — 고유 키는 이름이 아니라 `<이름>@<마켓플레이스>`
- [x] **E0.3** `claude mcp list`가 조회가 아니라 **전 서버 health-check**임을 확인 → 스냅샷
      입력에서 배제, 설정 파일 직독으로 전환
- [x] **E0.4** `--bare`가 인증까지 끄는 것을 실측 → 봉인에서 배제
- [x] **E0.5** `--safe-mode`가 인증을 유지하면서 커스터마이즈를 차단함을 확인 → `sealed-live`
      프로파일의 근거
- [x] **E0.6** 트랜스크립트 구조 실측 — 서브에이전트는 **별도 파일**(`subagents/agent-*.jsonl`),
      `attribution*` 필드 4종 존재
- [x] **E0.7** `sealed-live` churn baseline 측정(유료 3회) → Tier-2 허용목록 확정

**실측은 전부 `docs/harness-facts.md`에 있다.** 추측이나 도움말 인용만으로는 넣지 않는다.

---

## E1 — 기반: 스키마·계층·CI

- [x] **E1.1** pnpm 모노레포 골격 — `core`·`probe`·`actuator`·`sync`·`gen`·`cli`·`web`
- [x] **E1.2** 스키마 정의(zod) — `Asset`·`Installation`·`Machine`·`Project`·`UsageMetric`·
      `Annotation`·`DocPage`
- [x] **E1.3** **머신 종속/독립 분리** — Asset·Annotation은 전 머신 공유, Installation·Usage는
      머신별로 쌓임
- [x] **E1.4** 계층 경계를 **lint로 강제** — `core`는 I/O 없음, `probe`는 읽기만,
      `sync`가 카탈로그 유일 쓰기 주체
- [x] **E1.5** 런타임 의존성 0 게이트 — 승인 목록(`zod`·`@anthropic-ai/sdk`) 외 금지
- [x] **E1.6** 공개 저장소 위생 검사 — 추적 파일에서 홈 경로·머신명 검출
- [x] **E1.7** CI 파이프라인 — typecheck·lint·test·위생·의존성·공급망 감사
- [x] **E1.8** **CI가 실제로 도는지 확인** (#4) — 5회 전부 `Setup Node`에서 죽어 그 아래 게이트가
      한 번도 실행된 적 없었다. Node 22.13으로 수정하고 브랜치 보호로 머지 조건화
- [x] **E1.9** **`ctk` 실행 파일 복구** (#4) — `bin`이 `.ts`를 가리켜 설치해도 실행 불가였다

> ⚠️ E1.8·E1.9는 "테스트 통과 ≠ 실행됨"의 사례다. 224개가 통과하는 동안 빌드된 CLI가 죽어
> 있었다.

---

## E2 — 조회: 스캔과 카탈로그

**요구사항 1**(어떤 스코프/프로젝트에 설치돼 있나) · **요구사항 6**(GitHub 링크)

- [x] **E2.1** `ctk init` — 카탈로그 생성, 멱등
- [x] **E2.2** `ctk scan` — 플러그인·스킬·MCP·CLI 수집
- [x] **E2.3** **자산 유형별 스코프 판정** — 유형마다 파일 위치와 토글 방식이 다르다.
      하나로 추상화하지 않는다
- [x] **E2.4** MCP 4출처 통합 — user·local·project·플러그인 번들
- [x] **E2.5** 스냅샷 append-only 기록 — 덮어쓰지 않는다
- [x] **E2.6** `ctk doctor --drift` — 두 스냅샷 diff로 유입·이탈 계산
- [x] **E2.7** `ctk verify ac1` — 스냅샷 ↔ 재구성 독립 대조
- [x] **E2.8** 저장소 링크 수집 (#5) — `known_marketplaces.json`이 유일 출처.
      `javascript:` 등 위험 스킴 차단
- [x] **E2.9** **경로 원문 금지(AC-1.7)** — 카탈로그에 가는 값은 `path_hash`뿐

---

## E3 — 쓰기: 스코프 이관과 롤백

**요구사항 5**(제품에서 스코프 이관)

- [x] **E3.1** `ctk move --to user|project` — 플러그인 enablement 전이
- [x] **E3.2** 스킬 디렉터리 이동
- [x] **E3.3** **백업 → 수정 → 검증 → 실패 시 롤백** 강제
- [x] **E3.4** `ctk rollback --last` — 마지막 조치 되돌리기
- [x] **E3.5** MCP 이관 거부 · CLI 이관 미지원 — **각각 다른 사유**로 거부
- [x] **E3.6** 중단된 복원 탐지 — `ctk doctor`가 최우선으로 경보
- [x] **E3.7** `security-reviewer` 통과 (#2)

> **설치 스코프와 활성 여부는 다른 축이다.** v1의 "이관"은 **enablement 전이**이며 설치 위치는
> 바뀌지 않는다. 진짜 install scope 이동은 비범위(아래 참조).

---

## E4 — 사용량 측정

**요구사항 4**(사용량 지표)

- [x] **E4.1** 트랜스크립트 파서 — `~/.claude/projects/*.jsonl`
- [x] **E4.2** **서브에이전트 별도 파일 처리** — 메인 세션 파일에는 없다(실측 정정)
- [x] **E4.3** 귀속 4필드 활용 — `attributionSkill`·`Plugin`·`McpServer`·`Agent`
- [x] **E4.4** 상시 점유 측정 — `count_tokens` 실측
- [x] **E4.5** **3상태 강제** — `measured`/`unmeasured`/`approx_bytes`.
      추정치를 토큰 필드에 넣지 않는다
- [x] **E4.6** `ctk usage --unused-expensive N`
- [x] **E4.7** R17 서브에이전트 귀속 괴리 탐지 → `ctk doctor` 노출
- [x] **E4.8** **순위 자격 판정** (#14) — 크레덴셜이 없으면 비싼 자산이 미측정으로 빠지고
      *정말로* 0인 것만 순위에 남는다. 개별 숫자는 맞는데 질문에는 거짓이다.
      `ctk usage`와 웹 양쪽에서 **숫자보다 먼저** 고지

> ⚠️ E4.8은 웹 경로에만 배선돼 있다가 README 작업 중 CLI 누락이 발견됐다.

---

## E5 — 문서 자동 생성

**요구사항 2**(스폰 메타데이터) · **요구사항 3**(역할·목적) · **요구사항 7**(사용법 페이지)

- [x] **E5.1** `gen` 계층 — 스스로 파일을 쓰지 않고 산출물은 `sync` 경유
- [x] **E5.2** **서브프로세스 봉인** — 두 프로파일(`test-isolated`/`sealed-live`).
      인증 토큰이 config dir별로 분리돼 합칠 수 없다
- [x] **E5.3** env 화이트리스트 강제 — 상속 없이 명시 구성, 위반 시 spawn 전 거부
- [x] **E5.4** 프롬프트 봉투 — 매 실행 새 32바이트 구획자
- [x] **E5.5** 인용 강제(P5) — 모든 문단·불릿에 원본 라인 인용
- [x] **E5.6** 후검증 4규칙 — 인젝션 패턴 검출 시 `sync` 쓰기 **이전에** 거부
- [x] **E5.7** **2단계 비용 승인** — 견적 후 명시 승인 없이는 유료 호출 0
- [x] **E5.8** `--no-llm` 폴백 — 규칙 기반 추출
- [x] **E5.9** `--dry-run` — 네트워크·spawn 0
- [x] **E5.10** 트리 diff 검증 — 실제 config dir 대상, churn 허용목록 기준
- [x] **E5.11** managed 정책 게이트 — 위험 키가 있으면 비대화형 실행 거부
- [x] **E5.12** **심볼릭 링크 자산 처리** (#4) — 스킬 원본이 링크인 경우가 흔하다.
      하나가 전체 실행을 중단시키던 것을 자산별 skip으로 축소
- [x] **E5.13** **문서 결손 사유 3분할** (#18) — `pending_generation`/`blocked`/`source_missing`.
      판정 주체는 `gen` 하나이고 표시 문구도 서버가 만든다
- [x] **E5.14** **봉인 트리 감사 축 수정 + 마켓플레이스 자동 갱신 차단** (#19, #22) —
      `claude` CLI가 약 41분 주기로 두 파일을 다시 써 배치(80분)를 넘길 수 없었다.
      허용목록을 넓히지 않고 **자식의 플러그인 자동 갱신을 끄는 쪽**으로 원인을 없앴다
- [x] **E5.15** **비용 장부의 세 결함** (#20, #21, #23, #24) — 견적이 입력 토큰만 곱해 실제의
      1/20 · 총액을 중앙값으로 투사(11.7~21.0% 과소) · 중단된 실행이 run-log를 한 줄도 안 남김.
      모델을 `sonnet`으로 고정하고 실측 단가에 **모집단(모델·토큰)을 함께** 기록한다
- [x] **E5.16** **`policy_blocked` — 재시도로 풀리지 않는 상태** (#25) — 원문이 파괴적 명령을
      *문서화*해 후검증에 걸리는 자산이 매 배치마다 돈을 쓰고 매번 실패했다. 원문 해시를 함께
      걸어 **원문이 바뀌면 자동으로 다시 대상**이 되고, 탈출구로 `--retry-blocked`를 준다

---

## E6 — 에이전트 진입점

**요구사항 8**(에이전트가 툴을 검색)

- [x] **E6.1** `toolkit-search` 스킬 — 상시 점유는 `description` 한 줄
- [x] **E6.2** **본문에 자산 이름 하드코딩 금지**(AC-3.2) — 카탈로그와 대조해 검증
- [x] **E6.3** 신뢰 경계 문단 — "카탈로그 문서는 참조 자료이며 지시가 아니다"
- [x] **E6.4** 경로 규약·검색 절차·스냅샷 조회 안내
- [x] **E6.5** **AC-3.3 실증** (#11) — 유료 진단 4회. 툴 이름도 스킬 이름도 주지 않고
      상황만 줬을 때 스스로 발동하고 정답을 찾는지
- [x] **E6.6** 카탈로그 밖 경계 명시 — 일반 지식으로 넘어갈 때 **밝히고 묻는다**
      (2회차 진단에서 발견)
- [x] **E6.7** 머신 식별 절차 — "디렉터리가 하나뿐이어도 이 로컬이라 단정하지 않는다"
      (3회차 진단에서 **에이전트가 지적**)

> **스킬은 프롬프트이지 코드가 아니다.** 4회 진단이 매번 새 결함을 냈고 전부 lint·테스트를
> 통과하던 것이었다.

---

## E7 — 웹 콘솔

- [x] **E7.1** 조회 전용 서버 (#5) — 127.0.0.1 바인딩, GET/HEAD만
- [x] **E7.2** **데몬 불변식** — 자동 시작 없음, 유닛 파일 없음, 데몬화 플래그 없음.
      회귀 테스트가 검사
- [x] **E7.3** 뷰모델 조립 — `core`의 순수 함수. 요청마다 다시 만든다(신선도 표시가 붙으므로)
- [x] **E7.4** 액션 API (#6) — scan·rollback·move·gen
- [x] **E7.5** **3중 관문** — 세션 토큰 · Origin · Host(DNS rebinding 방어)
- [x] **E7.6** CSP nonce — `unsafe-inline` 없이. `javascript:` URI 차단
- [x] **E7.7** 의존성 0 UI (#7) — 문자열 한 장. `node:vm` 파싱 게이트로 회귀 방지
- [x] **E7.8** 프로젝트 이관 UI (#8) — **마지막 세그먼트만** 표시, 동명 충돌 시 상위 한 칸 +
      해시 접두
- [x] **E7.9** 인덱스 정합성 대조 — 화면이 본 프로젝트와 서버가 쓸 프로젝트가 같은지
- [x] **E7.10** 프로젝트명 노출 고지 + `--no-projects` (#9)
- [x] **E7.11** **`--open`** (#14) — 브라우저 자동 실행. 액션 모드에서는 토큰이 실행 인자에
      실리므로 매번 고지

---

## E8 — 보안 심사 대응

> `actuator`·`gen`·웹 액션은 사용자 환경을 바꾸므로 `security-reviewer`를 반드시 거친다.

- [x] **E8.1** High 3건 (#6) — 조회 경로 DNS rebinding · 승인 총액 오해석($2→$50) ·
      `javascript:` URI
- [x] **E8.2** Medium 전건 (#6~#8) — 견적 토큰 테스트 부재 · 인덱스 정합성 · 경로 유출 ·
      시각적 동일 라벨
- [x] **E8.3** Low 3건 (#10) — 위생 사각지대 · 확인 패널 중첩 · 포트 TOCTOU
- [x] **E8.4** 재심 L1 (#9) — 무인증 조회 채널의 프로젝트명 노출

### 반복해서 드러난 두 패턴

**"방어를 만든 것과 모든 경로에 배선한 것은 다르다"** — 8회 나왔다(사례는
`docs/harness-facts.md`). Origin 관문이 액션 분기에만 배선돼 조회가 무방비였고, managed 정책
탐지기가 `gen`에는 있고 `doctor`에는 없었고, 순위 경고가 웹에만 있었다.

**"없음과 판정 불가를 구분한다"** — 파싱 실패를 빈 결과로, 미실행을 "0건 일치"로 삼키는 코드가
반복해 나왔다. 판정할 수 없으면 에러를 던지거나 `unmeasured`로 남긴다.

---

## E9 — 릴리스 게이트

> 계획서 §6이 "릴리스 전 1회"로 지정한 항목. **셋 다 계획서에만 있고 어떤 표준 명령에도 없어서
> 한 번도 실행된 적이 없었다.**

- [x] **E9.1** **ⓓ-2 실행형 봉인 테스트** (#12) — 유료 3회, 4신호 전부 통과
  - [x] 양성 대조군(`--append-system-prompt` 주입 탐지)
  - [x] (i) SessionStart 훅 마커 미생성 — **대조군을 새로 만들었다.** 이전에는 마커를 만드는
        훅이 없어 봉인 여부와 무관하게 항상 통과였다
  - [x] (ii) 실제 `CLAUDE.md` 문자열 미로드
  - [x] (iii) 설치 플러그인 커맨드 미인식
- [x] **E9.2** **managed 정책 탐지** (#12) — `ctk doctor --managed-policy` 신설.
      파싱 실패를 "위험 없음"으로 삼키던 결함도 함께 수정
- [x] **E9.3** **AC-3.9 LLM 경로** (#12) — 악성 픽스처 5종을 실제 모델에 통과.
      전부 `sync` 쓰기 이전에 거부
- [x] **E9.4** **AC-3.5 수동 게이트** (#13, 스킬 경로 2026-08-26) — 플러그인 경로·스킬 경로
      **양쪽 통과**(허위 커맨드 0건 · 오발동 0건). 근거는 [남은 작업](#남은-작업)의 R2
- [x] **E9.5** 게이트를 `pnpm verify`·CI·`release:*` 스크립트에 **등록**

> ⚠️ E9.1의 (i)은 유료 실행 **직전에** 공허함이 발견됐다. 그대로 돌렸으면 아무것도 증명하지
> 않는 신호가 "통과"에 섞였을 것이다.

---

## E10 — 사용자 문서

- [x] **E10.1** README 전면 개편 (#14) — 5분 시작하기 · 상황별 안내 · 명령 레퍼런스 11개 전량
- [x] **E10.2** 「알아두면 좋은 것」 — 순위와 모집단, "없음"과 "판정 불가", 서드파티 원문의
      신뢰 경계, 문서가 인용한 원본만큼만 완전한 이유
- [x] **E10.3** **문서-CLI 계약 테스트** — README의 명령·플래그·스크립트가 실재하는지 검사.
      문서 드리프트는 조용히 생긴다
- [x] **E10.4** README 개인 데이터 부정 단언 — machine_id(UUID)·자산 id·스냅샷 파일명.
      **위생 검사는 경로 리터럴만 보므로 이 축을 못 잡는다**
- [x] **E10.5** 이 로드맵

---

## 남은 작업

| 분류 | 항목 | 성격 |
|---|---|---|
| ~~사용자 조치~~ | ~~R1~~ | **완료(2026-08-24)** — `ant auth login` 프로파일 |
| ~~미측정~~ | ~~R2~~ | **완료(2026-08-26)** — AC-3.5 스킬 경로 · AC-3.3 재검증 둘 다 통과 |
| 설계 부채 | R4 · R5 · R6 · **R10** | ~~R11~~ 완료(2026-08-26) · R10은 미측정 축으로 남음 |
| **백로그(사용성)** | ~~B1~~ · ~~B2~~ · **B3 · B4** | **B1 완료(2026-08-27)** — 편입+계층 표시. B4는 B1이 열어 준 문(번들 MCP·훅, 심사 후속 5건) |
| v1.1 | R7 | 선결 조건이 있다 |
| v2 | R8 · R9 | 어댑터 경계는 이미 열려 있다 |

> ⚠️ **이 표는 2026-08-25까지 실제 상태보다 뒤처져 있었다.** #22~#25가 `ROADMAP.md`를 한 줄도
> 건드리지 않아, 이미 사라진 「미실행 117건 · 위생 거부 52건」이 남은 일로 남아 있었다.
> **문서의 목적이 "컨텍스트 없이 이어받게 하는 것"이면 뒤처진 지도는 없는 지도보다 나쁘다** —
> 이어받는 사람이 이미 풀린 문제를 다시 푼다. 유료 배치처럼 **카탈로그만 나아가고 문서는
> 안 나아가는** 작업은 PR에 문서 갱신을 함께 싣는다.

### 완료 — 크레덴셜과 측정

- [x] **R1. count_tokens 크레덴셜 확보 후 `ctk measure`** (2026-08-24)
  - **어떻게 풀었나**: API 키 발급이 아니라 **`ant auth login` OAuth 프로파일**이다. SDK는
    `ANTHROPIC_API_KEY` → `ANTHROPIC_AUTH_TOKEN` → `ant` 프로파일 → WIF 순으로 해석한다
  - **결과**: Occupancy **전 자산 측정 완료**. `usage --unused-expensive`의 순위 자격 경고 해소
  - ⚠️ **막고 있던 것은 크레덴셜이 아니라 결함이었다** — `measure` 게이트가 env 하나만 봐서
    프로파일을 가진 사용자를 거부했다. `count-tokens.ts`는 이미 전체 체인에 위임하도록
    고쳐져 있었고 **CLI 게이트에만 배선이 빠져 있었다**(안전 원칙 5의 여덟 번째 재현)
  - **환경 전제**: 이 회선에는 TLS 가로채기 프록시가 있어 `NODE_EXTRA_CA_CERTS`로 루트 CA를
    지정해야 Node SDK 호출이 성립한다. 없으면 `measurement_failed(tls_chain_untrusted)`다

- [x] **R3. AC-3.1 상시 비용 상한** (2026-08-24)
  - **재보니 위반이었다** — 89토큰 / 상한 60. 미측정이 위반을 가리고 있었다
  - **해결**: `toolkit-search`의 `description` 한 줄만 영어로. 89 → **36토큰**.
    한국어는 문자당 약 2.9배의 토큰을 쓴다 — 영어안이 *더 긴 문장*인데 절반 이하다
  - **파생**: description이 바뀌었으므로 **AC-3.3 재검증 필요**(아래 R2와 묶어 실행)

### 완료 — 미측정으로 남아 있던 것

- [x] **R2. AC-3.5 스킬 경로 + AC-3.3 재검증** (2026-08-26 · 둘 다 통과)
  - **AC-3.5 스킬 경로** — 생성 문서의 「When to use」 문장을 그대로 입력해 대상 스킬이
    발동했다. 판정 근거는 하네스가 남긴 `Skill` 도구 호출이고 **오발동 0건**이다.
    AC-3.5는 이제 플러그인 경로와 스킬 경로 **양쪽 통과**다
  - **자기 참조를 피하는 핵심은 자동화 여부가 아니라 판정 주체다** — 문서가 "자동화하면
    자기 참조"라고 적은 것은 `ctk`가 자기 산출물을 파싱해 자기가 판정하는 경우다. 여기서는
    사람이 문구를 꺼내고 **하네스의 도구 호출 기록이 판정한다.** 새 세션 요건은 헤드리스로
    만족되고, 쓰기 도구를 막아 실환경 부작용도 없앴다
  - **AC-3.3 재검증** — 네 축(발견·설치 상태·경계·머신 식별) 전부 통과.
    **R3의 파생 우려가 해소됐다** — `description`을 89 → 36토큰으로 줄인 뒤에도 자율 발견이
    그대로 동작한다
  - **기록**: `docs/harness-facts.md`의 AC-3.5 · AC-3.3 절

### 설계 부채

- [ ] **R4. 안 A — 봉인에 전용 env 추가**
  - **무엇**: AC-3.3 진단이 지금은 **스킬 사본**(안 B)을 쓴다. 프로덕션 스킬 본문 그대로를
    시험하려면 `CTK_CATALOG_ROOT` 같은 전용 env를 화이트리스트에 등재해야 한다
  - **남은 미검증 범위**: "스킬이 `config.json`/`machine.json`을 실제로 읽는가" 한 절
  - **선결**: **봉인 변경이므로 `security-reviewer` 재심 필수**
  - **참고**: 안 B의 계약(정확히 한 절만 다름)은 `core/catalog/probe-skill.ts`에 코드로 갇혀 있다

- [ ] **R10. 봉인 트리 감사의 실환경 축 재측정** (2026-08-26 신규)
  - **무엇**: `.session-stats.json`이 등재돼 있는데도 제외되지 못하던 결함을 고쳤고
    (`attribution` 판별 유니온, `security-reviewer` 재심 Critical/High 0건), 유닛 43건과
    파괴 실험 6축으로 검증했다. **그러나 결함이 태어난 실환경 조건은 재현하지 못했다**
  - ⚠️ **관측이 사건을 없앴다**: 감사를 방해하지 않으려고 실행 중 아무 명령도 돌리지 않았더니
    세션 활동이 없어 그 파일이 **아예 갱신되지 않았다**(`session_owned_excluded`가 1로 그대로).
    재려면 실행 중 **의도적으로** 세션 활동을 만들어야 하는데 그러면 다른 churn이 위반을 낸다 —
    **두 요구가 맞서고 아직 방법이 없다**
  - **미측정은 통과가 아니다** — 릴리스 판정에서 "아직 실패일 수 있음"으로 센다
  - **함께 남은 것**: `hooks/logs/notifications.log`가 새 위반으로 드러났다(등재 안 됨).
    같은 부류가 몇 개 더 있을지 모른다

- [x] **R11. 유료 spawn 지점의 모델 미고정 · 지출 미기록** (2026-08-26 발견·수정)
  - **셋 중 하나에만 배선돼 있었다.** `claude -p`를 띄우는 자리를 전수 조사하니 `--model`은
    `gen` 한 곳뿐이었다 — #23이 `gen`을 고정할 때 진단(`agent-probe`)과 봉인 시험
    (`seal-live-test` 3호출)은 그대로였다. **한 자리를 고쳤으면 같은 형태의 다른 자리를 센다**
  - ⚠️ **`seal-live-test`의 누락은 릴리스 게이트를 죽일 수 있었다** — `pnpm release:seal`은
    `--max-budget-usd 0.50` 고정인데 자식이 사용자 기본 모델을 쓰면 상시 컨텍스트가 큰 머신에서
    호출 하나가 그 상한을 넘는다(실측 $0.863). 게이트가 **0원에 거부되고** 그것은 "통과"가
    아니라 **미측정**인데 그 구분이 사라진다. R11은 `agent-probe`만의 문제가 아니었다
  - **모델을 고정하는 이유는 비용이 아니라 모집단이다** — 없으면 진단 결과가 머신마다 달라져
    "지난번엔 통과했다"를 비교할 수 없다. 과거 AC-3.3 진단 4회가 어떤 모델이었는지 **지금
    알 수 없는 것**이 그 증거다. `--model`로 덮을 수 있고, 결과에 어떤 모델로 쟀는지 실린다
  - **상수는 셋으로 나눴다**(`GEN_MODEL`·`PROBE_MODEL`·`SEAL_TEST_MODEL`) — 값은 같지만 뜻이
    다르다. 합치면 한쪽 사정으로 바꿀 때 다른 쪽 판정 기준이 조용히 따라 움직인다
  - **지출은 화면에 남긴다** — `agent-probe`는 "`sync`를 전혀 호출하지 않는다"가 계약이라
    run-log에 쓸 수 없다. **계약이 저장 위치를 정하고, 저장할 수 없으면 표시가 유일한 자리다.**
    읽지 못한 값은 "미보고"이지 0원이 아니다. 요청 모델과 실제 모델이 다르면 그것도 드러낸다
  - **파괴 실험 5축 전부 잡힌다** — 진단 `--model` 제거 · 봉인 **세 자리 중 하나만** 제거 ·
    미보고를 0원으로 · 모델 불일치 숨김 · 봉투 파싱 실패를 빈 문자열로. 부분 배선이 통과하지
    못한다는 것이 (b)로 확인됐다
  - ⚠️ **작업 중 백업을 스스로 깨뜨렸다** — `packages/gen/src/agent-probe.ts`와
    `packages/cli/src/commands/agent-probe.ts`를 한 디렉터리에 `cp`했더니 **파일명이 같아**
    뒤엣것이 앞엣것을 덮었고, 복원 때 gen 쪽에 cli 내용이 들어갔다. `main`에서 되살려 복구했다.
    **백업에도 이름 충돌 축이 있다** — 경로를 보존해 뜬다(`tar` 또는 `cp --parents`)

  - **견적 축이 하나 더 있다**: AC-3.5 진단 1회의 `cache_read`가 218K였다. **진단 비용은 자산
    크기가 아니라 그 머신의 상시 컨텍스트가 지배한다** — 이 제품이 푸는 문제 2가 비용으로
    나타난 것이다. **AC-3.3의 과거 진단 4회 지출은 지금도 알 수 없다**(기록이 없었다)

- [x] **R12. `policy_blocked`가 CLI 요약에서 `stale`로 표시되던 결함** (2026-08-26 발견·수정)
  - **무엇**: #25가 인덱스(`gen_state`)는 `policy_blocked`로 갈랐는데 **결과 객체(`outcome`)는
    `stale` 그대로였다.** `GenAssetOutcome` 유니온에 그 값이 아예 없었다 — **좁힌 타입이 축을
    지웠다.** 그래서 재시도 루프를 실제로 끊어 놓고도 화면은 "갱신필요 3건"이라 말해
    **재시도를 권했다.** #25의 목적이 사용자 눈에는 달성되지 않은 상태였다
  - **안전 원칙 7의 거울상** — "표시를 갈랐으면 값을 만드는 계층도 갈렸는지 본다"의 반대로,
    **값을 갈랐는데 표시가 안 갈렸다.** 양방향으로 봐야 한다
  - ⚠️ **테스트 두 곳이 결함을 고정하고 있었다.** `outcome: "stale"`을 못박은 단언이 둘 있었고,
    그중 하나는 **바로 아래 줄에서 인덱스가 `policy_blocked`인지 확인하고 있었다** — 한 테스트
    안에서 두 계층이 어긋난 채 통과했다. 세 번째 자리(`citation_missing`)는 재시도로 풀리므로
    `stale`이 맞다 — **범위를 재서 건드리지 않았다**
  - ⚠️ **요약 문자열을 태우는 테스트가 하나도 없었다.** `bin/ctk.ts` 인라인이라 태울 수 없었고,
    카운트가 `filter(...).length`라 새 outcome이 **어디에도 안 잡혀도 타입이 막지 않았다**(고치는
    순간 3건이 요약에서 통째로 사라졌다). 함수로 빼고(`countGenOutcomes`·`describeGenSummary`)
    `switch`로 완전성을 강제했다 — **outcome이 늘면 컴파일이 깨진다.**
    합이 입력 건수와 같은지 보는 테스트를 넣었다(개별 카운터만 단언하면 "안 잡힘"이 통과한다)

- [ ] **R5. 데몬 불변식 검사의 오탐**
  - **무엇**: `detached: true` 검사가 문자열 매칭이라 **그 패턴을 인용한 주석도 잡는다**.
    실제로 걸렸다
  - **방향**: AST 기반으로 바꾸거나, 오탐이 fail-closed임을 문서로 남기고 유지
  - **급하지 않음**: 오탐 방향이 안전하다

- [ ] **R6. 확인 패널 중첩 시 UX**
  - **무엇**: 현재 확인이 열려 있으면 다른 액션을 막고 이유를 패널에 표시한다.
    "앞 것을 취소하고 새로 열기"가 더 자연스러울 수 있으나, 그러면 깨어난 앞 프레임이 새 패널을
    지우는 문제가 있다(#10에서 검토)
  - **급하지 않음**: 현재 동작이 안전하고 빠져나갈 길이 있다

### 백로그 — 사용성 개선

사용자가 콘솔을 실제로 써 보고 나온 요구다. **순서는 의존성 순이다** — B1이 자산 목록의
구조를 바꾸고, B2가 그 목록에 실릴 내용을 채우고, B3이 그 위에 디자인을 입힌다.

- [x] **B1. 플러그인 하위 툴을 카탈로그에 편입하고 계층으로 표시** (2026-08-27 완료)
  - ⚠️ **전제 정정(2026-08-26)**: 이전 항목은 "번들한 스킬·MCP·커맨드가 최상위 행으로
    평평하게 나열된다"고 적었다. **틀렸다 — 하나도 나열되지 않는다.** 번들 스킬은 스캔
    루트 밖이고, 번들 커맨드·에이전트는 수집기 자체가 없으며, 번들 MCP는 의도적으로
    비-Asset이다(「결정 7」). **B1은 UI 작업이 아니라 카탈로그 편입 작업이었고, 계층 표시는
    편입의 귀결이다.**
  - **왜 놓쳤나**: *"번들인지 구분되지 않는다"*는 관측을 **축**(계층 표시 부재)으로 읽었으나
    원인은 **범위**(수집 범위 밖)였다. 축으로 읽으면 `web` 작업, 범위로 읽으면 `probe`부터
    `gen` 비용까지 성격이 달라진다 — CLAUDE.md 「"0건"은 범위와 축을 함께 의심한다」의 재현.
  - **한 일**: 플러그인이 번들한 **스킬·커맨드·에이전트 3종**을 부모 참조를 가진 1급 Asset으로
    편입하고 웹 계층 뷰까지 배선했다. **에이전트는 이전 항목에 존재조차 하지 않았다.** MCP는
    「결정 7」 유지, 훅은 자동 발동이라 제외했다.
  - **규모**: 편입 후 카탈로그 자산 수가 **약 6~7배**가 됐다. 유료 문서 생성은 **자동으로
    발생하지 않는다** — 플러그인 단위 명시 지정(`--plugin`)일 때만 대상이 된다.
  - ⚠️ **선행 결함(해소함)**: 카탈로그 파일 경로가 `(kind, name)` 축인데 id 충돌 방지는 id 축에만
    있었다. 번들 하위 툴에는 **이름 충돌이 실재했다**(플러그인만 있을 때는 0건이라 한 번도
    터지지 않았다). 편입 전에 경로 축부터 고쳤다(Step 1) — 안 그러면 자산이 조용히 사라진다.
  - **주의**(유지): 머신 독립/종속 축을 섞지 않는다. 표시 계층 ≠ 토글 의미.
  - **정정**: 이전 항목의 *"하위 툴도 독립적으로 설치·활성될 수 있다"*는 id 축에서 해소됐다 —
    같은 이름의 번들과 독립 설치는 **애초에 서로 다른 자산**이다. 자식은 `Installation`을
    갖지 않고 부모에서 상속하며, 뷰가 그것을 **빈 배열과 구분되는 값**으로 낸다.
  - **커밋**: `eca79b1`(Step 1 경로 축) · `a19439f`(Step 3 부모 축) · `c82ed71`(Step 5 편입 본체,
    security-reviewer 2회) · `19a6951`(Step 5 원문 리졸버, security-reviewer 3회) ·
    `0aaa83f`(Step 6 view 3-arm + web 계층)
  - **계획서**: `.omc/plans/ctk-b1-plan.md`
  - ⚠️ **후속이 남아 있다** — 보안 심사가 남긴 M-B·L-A~L-D와 원래 B4 후보는 **B4**로 별도
    등재했다(아래 백로그 끝)

- [x] **B2. 문서가 없는 사유를 파악하고 채우기** (2026-08-26 완료)
  - **무엇**: 콘솔 자산 상세 대부분이 "문서가 아직 생성되지 않았다"로 뜬다. 사유를 갈라 각각
    다르게 처리한다
  - ✅ **실측(2026-08-26) — 생성은 사실상 끝났다.** 생성 대상 중 **98%가 `fresh`**이고
    `annotation.md` 실제 파일 수와 일치한다. 나머지는 **`stale` 3건**(아래)과
    **유형상 원문 부재**(mcp·cli — 원문이 없어 생성 대상이 아니다)뿐이다.

    **아래에 남아 있던 「미실행 · 위생 거부 · 원본 없음」 세 무리는 전부 해소됐다.**
    2026-08-23 시점의 분포이고 지금 남은 일이 아니다 — 이력으로만 읽는다.
  - ⚠️ **남은 3건은 비용 문제가 아니라 정책 문제다.** **원문 grep으로 확인**했다:
    셋 다 `rm -rf`·`sudo`·`You MUST`를 본문에 담고 있다(안전·훅 계열 자산이 파괴적 명령을
    *문서화*한 경우다). #25가 만든 `policy_blocked`의 정의 그대로이고, #25 커밋이 말한
    "매 배치마다 돈을 쓰고 매번 실패한 3건"과 **건수가 일치**한다. 지금 `stale`인 이유는
    **#25 머지 뒤 `gen`을 아직 한 번도 돌리지 않아** 마킹이 안 됐기 때문이다.
    남은 일은 "문서를 더 만드는 것"이 아니라 **한 번 돌려 상태를 확정해 재시도 루프에서
    빼는 것**이다
  - [x] ✅ **완료(2026-08-26) — 순수 터미널에서 1회로 끝났다.** 사용자가 Claude Code를 종료하고
    `pnpm ctk gen --max-assets 3 --max-budget-usd 0.30 --timeout-sec 300`을 돌렸다.
    3건 전부 `injection_pattern_detected`로 **인덱스에 `policy_blocked`가 찍혔다** — 예측대로다.
    실측 비용 **$0.21**(견적 $0.16 · 자산당 중앙값 $0.067), 인젝션 후검증은 **실행명령 4건**.
    문서는 생기지 않지만 **재시도 루프에서 빠졌다**(#25의 목적 달성).
    이로써 카탈로그의 모든 자산이 세 상태 중 하나로 확정됐다 — `fresh` · `policy_blocked` ·
    유형상 원문 부재
  - ⚠️ **그 실행이 새 결함을 드러냈다 — 화면은 여전히 재시도를 권하고 있었다.** 아래 R12 참조
  - **막고 있던 것은 대상 자산이 아니라 살아 있는 세션이었다(시도 2회 · $0.135 지출 · 문서 0건).**
    막은 것은 대상 자산이 아니라 **살아 있는 Claude Code 세션의 churn**이었다. 1회차는
    `.session-stats.json`(아래 결함), 2회차는 `hooks/logs/notifications.log` — **시도마다 다른
    파일이 나오고 매 시도가 유료다.** 항목을 하나씩 등재하는 것은 두더지 잡기이므로
    (`docs/harness-facts.md`의 「클록 넷」이 닿지 않는 축을 이미 예견해 뒀다) 중단했다.
    **처리하려면 Claude Code를 종료하고 순수 터미널에서 돌린다** — 그것이 원래 권장 절차다
  - ⚠️ **이 절을 쓰면서 위생 규칙을 한 번 어겼다가 되돌렸다** — 자산 **실명 3개**와 총 설치
    규모를 적었는데, `scripts/hygiene-check.mjs`는 주석에 적힌 대로 **설치 목록·개인 사용량
    수치를 검출 대상에서 스스로 뺀다.** 돌렸으면 그대로 통과했을 것이다.
    **게이트가 보는 축과 새는 축이 다르다**(CLAUDE.md「0건은 범위와 축을 함께 의심한다」).
    카탈로그 실측을 문서에 옮길 때는 **이름을 빼고 비율로** 쓴다
    - **범위로 닫지 못했다(미결)**: 이 규칙을 새로 세웠으니 기존 서술도 같은 축으로 봐야 하는데,
      B2·B2-b의 이력 문단에는 **절대 건수가 그대로** 남아 있다(설치 규모 축). 실명이 아니라
      규모만 드러내므로 위험은 낮다고 보고 이번에는 손대지 않았다 — **판단이지 검증이 아니다.**
      전수 정리 여부는 별도로 결정한다
  - **실측(2026-08-23 · 이력)** — 아래는 착수 시점의 분포다. **위 2026-08-26 실측이 현재값이고,
    이 절은 왜 그렇게 갈랐는지를 남기기 위해 보존한다.** 전 자산 중 문서 생성 완료는 **2건**뿐이었다.
    `ctk gen --dry-run` 기준 사유는 셋으로 갈린다:

    | 사유 | 건수 | 성격 |
    |---|---|---|
    | **미실행** | 117 | `gen`을 끝까지 돌리지 않았다 — 유료다 |
    | **원본 없음** | 12 | 자산은 있는데 `SKILL.md`·`README`를 못 찾았다 |
    | **위생 거부** | 52 | 전부 `path_traversal_detected` — 원본이 **심볼릭 링크** |

  - **각각 다른 작업이다:**
    - 117건 → **비용 문제.** 예산과 승인만 있으면 된다. `--max-assets`로 나눠 돌릴 수 있다
    - 12건 → **탐색 문제.** ✅ **B2-b 완료(2026-08-24)** — 아래 참조. 드리프트는 **0건**이었다
    - ✅ **URL 정책 결정(2026-08-24)** — 생성 문서의 비허용 링크를 **거부에서 제거로** 바꿨다.
      실측: 남은 84건 중 **37건(44%)** 원문에 비허용 URL(총 320건)이 있어 매 배치가 돈을 쓰고
      같은 이유로 실패했다. 지금은 `[링크 생략]`으로 치환하고 문서는 저장하며, 제거 건수·호스트를
      run-log에 남긴다. **보안 심사에서 High 2건**(제거가 `curl_pipe_shell`을 무력화 · userinfo
      `@` 2개 이상에서 진짜 호스트 잔존)을 받아 근본 처방(원문 선판정)까지 넣었다.
      상세는 `docs/harness-facts.md`
  - ⚠️ **관찰 — 지시문·실행명령 규칙도 재시도 루프를 만들 수 있다(2026-08-24).** 남은 61건 중
    **6건**의 원문이 규칙에 걸린다(`you_must_directive` 3 · `rm_rf` 3 · `sudo` 1 — 대개 README가
    파괴적 명령을 **문서화**한 것이다). 3회차에서 그중 2건이 `injection_pattern_detected`로
    떨어졌다. **URL과 달리 확정된 루프는 아니다** — 모델이 그 토큰을 인용할지가 매번 달라
    재시도가 성공할 수 있고, `rm -rf`는 제거하면 의미가 바뀌어 URL처럼 지울 수도 없다.
    빈도가 올라가면 "정책상 생성 불가"로 표시해 재시도에서 빼는 쪽을 검토한다
    - 52건 → **정책 문제.** `probe`는 심볼릭 링크를 따라가고 `gen`은 거부한다
      (`docs/harness-facts.md`). 스킬 원본이 링크인 것은 **흔한 정상 배치**인데 지금은 문서를
      영영 못 만든다. 링크 대상이 홈 안이면 허용하는 식의 완화를 검토하되,
      **`~/.ssh/id_rsa`로 링크된 `SKILL.md`가 카탈로그에 박히는 것**을 막는 원래 이유는 지킨다
  - [x] **표시 3분할 완료 (2026-08-24)** — 자산 상세가 사유별로 **다른 문구와 다른 "할 일"**을
    보여준다. 실제 서버 응답으로 확인: `pending_generation` 117 · `blocked` 52 ·
    `source_missing` 12 · `generated` 2
    - **저장하지 않고 계산한다** — `source_missing`·`blocked`는 **이 머신의 파일 배치**에 대한
      사실이라 머신 독립 카탈로그에 넣으면 스키마의 축을 섞는다. 상세를 열 때 그 자산 하나만
      판정한다(전 자산 일괄 계산은 약 0.8초라 요청마다 얹을 수 없다)
    - **판정 주체는 `gen` 하나** — 단건 조회와 일괄 산출이 같은 함수를 타고, 갈리면 실패하는
      게이트가 있다(`plan.test.ts`). 표시 문구도 서버가 만든다(브라우저가 따로 조립하면 이원화)
  - **남은 것은 아래 셋이다** — 표시가 갈렸으니 이제 각각을 실제로 처리하는 일이 남았다
  - [x] **B2-b 완료(2026-08-24) — "원본 없음" 12건 분해.** 가설(드리프트냐 리졸버 결함이냐)이
    **둘 다 틀렸다.** 실측 분류: 드리프트 **0건** · 중복 설치 6건(`SKILL.md` 해시 전부 일치) ·
    유형상 원문 부재 6건(mcp 4·cli 2, `description`이 유형 전체에서 0%)
    - **`empty: boolean` 하나가 다섯 갈래를 뭉개고 있었다** — B2 표시를 3분할했을 때 한 계층
      위에서 고친 것과 **같은 결함이 한 계층 아래에** 있었다. 판별 유니온으로 바꾸니 호출
      지점 8곳이 전부 컴파일에서 걸렸다(안전 원칙 5 — 필수 필드로 두면 누락이 통과하지 못한다)
    - **읽기 축과 쓰기 축을 갈랐다** — `findSkillDirsById`가 2건 이상을 거부하는 근거(H6)는
      "어느 디렉터리를 **이동**시킬지 모른다"는 쓰기 축의 판단인데 `gen`은 읽기다. 내용이
      바이트 동일하면 채택하고, 다르면 `ambiguous_source`로 여전히 거부한다.
      **위생 거부가 이긴다** — 중복 중 하나가 링크면 링크 아닌 사본을 골라 우회하지 않는다
    - **실측 변화**: 대상 112 → **116** · 위생 거부 52 → **54** · 유형상 원문 부재 **6** ·
      최신 7 (합 183). 중복 6건 중 2건은 `SKILL.md`가 서로를 가리키는 링크였다
    - 사유별 배지 문구의 출처는 `core` 하나이고, UI 스크립트에는 렌더 시점에 주입한다 —
      **실행 테스트로 확인**했다(견적 행 조립부는 그때까지 어떤 테스트도 실행하지 않았다).
      파괴 실험 7종(표시 매핑·판정 로직·유형·안전·라벨 주입·뭉갬 복원) 전부 잡혔다
  - **B2-a 진행(2026-08-25)**: 문서 **약 63%** 완료. 이 배치는 앞선 것들보다 **자산당 2~3배**
    느렸다 — 남은 자산의 원문이 평균적으로 더 크고, **Claude Code 세션 안에서 돌리면 트리
    감사가 세션 churn과 경쟁**한다(churn 감지율 12% → 100%). 온전한 감사와 제 속도를 원하면
    Claude Code를 종료하고 일반 터미널에서 배치 스크립트를 돌린다
  - ⚠️ **`claude` CLI 업데이트가 배치를 막았다(2026-08-25)** — 2.1.241 → 2.1.243으로 올라가
    `seal_unverified_cli`로 **0원에 거부**됐다. 복구 경로(`ctk verify seal`)가 실제로 작동해
    9.75초에 재증명하고 기록을 갱신했다. **그 통과에는 전제가 있다** — (i)의 양성 대조군
    SessionStart 훅이 실환경에 배선돼 있어야 한다. 상세는 `docs/harness-facts.md`
  - ⚠️ **public 저장소 위생 — 위생 게이트가 안 보는 축이 있었다(2026-08-25 · 정리 완료)**:
    `hygiene-check`는 경로 패턴만 잡고 **개인 사용량 수치는 스스로 검출 대상에서 빼 둔다**
    (스크립트 주석에 명시). 그래서 절대 지출액·설치 규모가 "위반 0건"을 통과했다. 문서·소스
    주석·테스트 픽스처 전수를 비율/합성으로 바꾸고 원본은 private 운영 저장소로 옮겼다
  - ⚠️ **견적이 총액을 중앙값으로 투사하고 있었다(2026-08-25 · 수정 완료)** — 실측 3배치에서
    **11.7~21.0% 낮게** 말했다. 분포가 오른쪽으로 길어(최대가 중앙값의 약 4.1배) 합계는
    건수 × **평균**이어야 한다. 평균은 이미 저장돼 있었고(총액÷보고건수) 곱하는 값만 틀렸다.
    **표시 계층 둘이 똑같이 틀려** 있어서 투사를 `core` 하나로 모았다
  - **B2-a 진행(2026-08-24)**: 파일럿 → 배치 5회로 문서 **약 절반**을 채웠다. 실측 단가는 두
    축이다 — **시간** 자산당 약 2분(대부분이 트리 감사: config dir 전체를 자산마다 전/후 2회
    스캔한다), **비용** 자산당 중앙값 기준. 배치 절차는 사용자 설정 디렉터리의 배치 스크립트다
  - ⚠️ **비용 견적 결함을 발견해 고쳤다(2026-08-24)** — 승인 화면의 "근사 비용"이 입력 토큰만
    곱한 값이라 **실제의 약 1/20**이었다. 지금은 하한·상한·실측 셋으로 갈라 보고하고, 실측
    단가는 run-log에 쌓아 다음 실행의 견적이 읽는다. `docs/harness-facts.md` 참조
  - **`--max-budget-usd`는 사전 견적으로 호출을 거부한다** — 1회차에서 30건 중 15건이
    1.5초·0토큰으로 거부됐고 **그 호출들에는 돈이 나가지 않았다.** 낮은 상한은 "비싼 자산을
    건너뛰는 필터"로 동작한다(다만 그 자산들은 상한을 올리기 전에는 영영 생성되지 않는다)
  - ✅ **봉인 회복(15:58 실측)** — 14:05~15:50의 봉인 행이 약 1시간 50분 만에 **스스로 풀렸다**
    (5초 응답). 코드·CLI 버전·argv 무엇도 바뀌지 않았고 **원인은 여전히 미상**이다. 재현 조건을
    모르므로 `docs/harness-facts.md`의 항목은 이분표와 무료 확인 절차와 함께 남겨둔다.
    **배치 실행이 다시 가능하다** — 남은 112건

- [ ] **B3. 콘솔 UI 디자인**
  - **무엇**: 현재 UI는 기능 위주다. 읽기 쉬운 시각 계층·여백·타이포를 입힌다
  - **제약**: **런타임 의존성 0을 깨지 않는다.** UI는 문자열 한 장이고 CSS는 인라인이다
    (프레임워크·번들러를 들이면 `deps:check`가 막는다)
  - **제약**: **CSP nonce를 유지한다.** `unsafe-inline`을 script에 열지 않는다 —
    `javascript:` URI가 CSP 층에서 허용되는 것이 보안 심사 High였다
  - **지키던 것을 잃지 않는다**(회귀 주의):
    - `unmeasured`를 "0"으로 보이게 만들지 않는다
    - MCP의 `unknown`(모름)과 `unset`(설정 안 됨)을 같은 배지로 칠하지 않는다
    - 순위 자격 경고를 **순위표보다 먼저** 띄운다
    - 값은 전부 `textContent`로 넣는다(`innerHTML` 금지 — 카탈로그 문서는 서드파티 원문 기반)
  - **선행 권장**: B1(계층)과 B2(문서 채우기) 뒤가 낫다. 표시할 구조와 내용이 정해진 뒤에
    디자인하는 편이 다시 그리는 일이 적다
  - **검증**: `node:vm` 파싱 게이트가 이미 있다 — 문자열로 조립한 스크립트는 **파싱까지가
    아니라 최소 스텁 위에서 실행해** 확인한다(과거 두 번 `SyntaxError`로 깨진 채 752개가 통과했다)

- [ ] **B4. 번들 MCP·훅 편입 재검토 + 보안 심사 후속**
  - **B1 이후에만 가능**: 번들 에이전트가 Asset이 됐으므로 사용량 귀속의
    `subagent_attribution: "unresolved"`를 실제 자산 id로 해석할 수 있다(`measure.ts:291`)
  - **B4-a — 번들 MCP·훅의 편입**: 「결정 7」 재검토. `.mcp.json`은 **두 형태가 공존한다**
    (아래 harness-facts 참조) — 파서를 만들면 그 사실부터 반영한다
  - **B4-c — `docs/workflow-assets.md`를 카탈로그에서 생성**: B1이 편입을 마쳤으므로 가능해진
    문이지만 이번엔 하지 않았다(이 저장소가 문제 1을 손으로 우회해 온 표다)
  - **B1 보안 심사가 남긴 것**(선재 결함 포함, B1이 새로 만든 것 아님):
    - **M-B** — `findPluginInstallPath`가 `validateInstallPath`를 거치지 않는다. 번들 축은
      3중 방어인데 플러그인 축은 미배선이다. `installed_plugins.json`이 오염되면 `gen`이
      임의 경로의 README를 읽어 카탈로그 문서에 넣고 sync 저장소로 내보낼 수 있다
    - **L-A** — frontmatter 스캔의 64KB 절삭이 판정을 바꾼다(harness-facts 실측). 닫는
      구획자 기준으로 자르거나 그 파일을 `unmeasured`로 남긴다
    - **L-B** — `oversizeTruncated`가 `gen`·웹 축에 미배선이다(생산 코드 소비자 0)
    - **L-C** — 번들 위치 캐시가 `validateInstallPath`를 배치당 1회로 줄여 TOCTOU 창이
      넓어진 사실이 문서화되지 않았다
    - **L-D** — `asset_source_not_a_file`이 `FAILURE_CLASSES`에 미등재이고, `blocked` 복구
      안내가 심볼릭 링크 얘기만 해서 FIFO로 막힌 사용자에게 엉뚱한 처방을 준다

### v1.1 후보

- [ ] **R7. MCP 프로젝트별 토글 쓰기**
  - **선결 ①**: `enabledMcpServers`/`disabledMcpServers` 두 키가 동시 존재할 때의 우선순위와
    각 키의 대상 범위 확정. 모르면 **쓰기가 곧 오작동**이다
  - **선결 ②**: `~/.claude.json` 단일 대형 JSON 편집의 안전 절차 — 한 파일에 전 프로젝트 상태가
    모여 있어 부분 손상의 폭발 반경이 크다. 원자적 쓰기 + 의미 diff 화이트리스트를 MCP
    서브트리에도 적용하는 설계가 선행돼야 한다

### v2 후보

- [ ] **R8. 카탈로그 원격 저장소**
  - **무엇**: `ctk init --catalog <url>` + 가시성 검사 + push/pull. v1은 **로컬 전용**이다
  - **승계**: `sync`가 어댑터 경계로 이미 분리돼 있다. Supabase 어댑터를 추가하면 클라우드로
    이행한다
  - **주의**: 동기화 저장소는 **반드시 private**. 스냅샷에 설치 목록과 사용량이 들어간다

- [ ] **R9. 멀티 로컬 동기화 UI**
  - **현재**: 스키마는 대비돼 있다(머신 종속/독립 분리). UI와 push/pull 흐름이 없다
  - **주의**: 조치는 **현재 로컬에서만** 실행한다는 원칙을 깨지 않아야 한다.
    타 머신 값은 그 머신이 마지막으로 push한 시점의 것이다

---

## 비범위

명시적으로 **하지 않기로** 한 것. 다시 논의하려면 그 근거부터 본다.

| 항목 | 이유 |
|---|---|
| 플러그인 **진짜 install scope 이동** | `uninstall` + `install`이며 되돌리기 어렵다. v1은 되돌릴 수 있는 enablement 전이만 |
| CLI 도구 이관 | 이관 개념이 없다 |
| 클라우드 백엔드 | v1은 로컬 우선. `sync` 어댑터로 경로만 열어 둠 |
| 훅·cron 자동 스캔/생성 | **수동 트리거만.** 사용자가 모르는 사이 유료 세션이 뜨면 안 된다 |
| 에이전트용 MCP 서버 | 상시 컨텍스트 비용이 든다. 스킬 `description` 한 줄로 대체 |
| 수동 문서 작성 워크플로우 | 카탈로그 문서는 **전부 자동 생성**한다 |
| `ctk`가 스킬을 대신 설치 | 사용자 실제 설정 디렉터리에 쓰는 코드는 백업·롤백·심사가 필요하다. 한 번 하는 일에 그 표면을 만들 이유가 없다 |

---

## 이어받는 사람에게

1. **`pnpm verify`가 표준 검증 하나다.** 게이트를 새로 만들면 **그 자리에서** 여기 등록한다 —
   계획서에만 있고 명령에 없던 게이트가 이번 v1에서 셋이나 한 번도 실행되지 않았다.
2. **유료 실행은 소요 시간으로 검산한다.** 유료 5회가 초 단위로 끝나면 모델은 돌지 않은 것이다.
3. **"통과"를 "검증됨"으로 읽지 않는다.** 이 프로젝트에서 찾은 결함은 대부분 *통과하고 있던*
   것들이었다.
4. 판단 기준은 `CLAUDE.md`, 실측 사실은 `docs/harness-facts.md`, 상세 계획은 `.omc/plans/`.
