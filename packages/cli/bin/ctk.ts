#!/usr/bin/env node
// ctk CLI 진입점 — 자리만. 커맨드 표면 정의는 plan §4.1 "CLI 표면 정의" 표를 따른다.
// init · scan · measure · gen · agent-probe · move · rollback · usage · web · doctor · verify.
// Step 2부터 순차 구현 (scan이 최초).

export {};
