# 의존성
node_modules/
.pnpm-store/

# 빌드
dist/
build/
.next/
out/
*.tsbuildinfo

# 환경
.env
.env.*
!.env.example

# 로컬 개인 설정
# ⚠️ 추적이 시작되기 전에 차단해야 한다 — .gitignore는 미추적 파일에만 작용한다
.claude/settings.local.json

# 개인 환경 데이터 — 이 저장소는 public이다. 스냅샷은 별도 private 저장소로 간다
snapshots/
*.snapshot.json
.ctk/

# OS / 에디터
.DS_Store
.vscode/
.idea/

# 런타임 상태
.omc/
.playwright-mcp/
