# ctk spike marker CLAUDE.md

CTK_SPIKE_CLAUDE_MD_MARKER_9f3a1c

This file exists only to detect whether the isolated-tree user-level
CLAUDE.md is auto-discovered and loaded into the system prompt when a
`claude` session runs without `--bare` but with `HOME`/`CLAUDE_CONFIG_DIR`
pointed at this isolated fixture tree (AC-0.10ⓓ). It carries no real
instructions and is not part of any production behavior.

If you are given this file as context, mention the exact string
`CTK_SPIKE_CLAUDE_MD_MARKER_9f3a1c` once at the start of your reply.
