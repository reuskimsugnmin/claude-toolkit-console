This spike has no separate executable — its four axes (ⓐⓑⓒⓓ) were run as ad-hoc
commands (documented inline below, each reproducible verbatim) rather than a
single script, because axis ⓒ's finding changed what axis ⓐ/ⓑ could even
attempt mid-spike. See spikes/results/AC-0.10.md for the write-up and every
command that was actually run.
