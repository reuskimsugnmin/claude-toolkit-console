# claude-toolkit-console

Claude Code 도구 환경(플러그인·스킬·MCP·CLI)을 **여러 로컬에 걸쳐** 보고, 배치하고, 옮기는
로컬 우선 웹 콘솔.

## 이 제품이 푸는 문제

1. **툴 선택** — 설치한 툴이 많아 어떤 상황에 무엇을 써야 할지 모른다. 툴 메타데이터만으로는
   자동 스폰에 한계가 있으므로, 사람이 **명시적으로 지시할 수 있도록** 툴별 사용 맥락을 보여준다.
2. **스코프 배치** — 전역 설치가 많아 기본 컨텍스트 비용이 과다하다. 전역에 두되 필요한
   프로젝트로 **스코프를 옮기고**, 한 프로젝트에만 있는 툴을 **다른 프로젝트로 복사**한다.
3. **로컬 간 가시성** — 다른 머신에서 이어 작업할 때 그 툴이 여기도 깔려 있는지 헷갈린다.
   **머신별 설치 현황**을 한 화면에서 본다.

## 아키텍처 결정 — 로컬 우선 + 동기화 어댑터

- **브라우저는 로컬 파일을 못 쓴다.** 따라서 로컬 에이전트(CLI)가 필수이고, 웹은 그 위의
  표현·조작 계층이다. 이 전제는 협상 대상이 아니다.
- v1 동기화 백엔드는 **Git 저장소**(백엔드·인증·비용 0, 오프라인 동작)이고, `sync`를 **어댑터
  경계**로 분리해 나중에 클라우드로 이행한다.
- 조치(스코프 이동 등)는 **현재 로컬에서만** 실행한다. 타 로컬은 마지막 push 시점으로 조회한다.

### 계층 경계

| 계층 | 책임 | 위험도 |
|---|---|---|
| `core` | 스키마·타입·스냅샷 diff. 순수 로직, I/O 없음 | 없음 |
| `probe` | 로컬 설정 **읽기**만 | 낮음 |
| `actuator` | 로컬 설정 **쓰기** — 백업·롤백 필수 | **높음** |
| `sync` | 스냅샷 push/pull 어댑터 · 카탈로그 저장소 **유일한 쓰기 주체** | 중간 |
| `gen` | LLM으로 카탈로그 문서 생성 — 서브프로세스 봉인 필수, 직접 쓰기 금지 | 중간(비용) · **봉인 실패 시 높음** · 실제 config dir에 churn 발생 |
| `cli` · `web` | 오케스트레이션 · 표현/조작 UI | — |

`gen`은 헤드리스 에이전트를 스폰하므로 **잠재적 쓰기 범위가 `actuator`의 화이트리스트보다 넓다.**
스스로는 어떤 파일도 쓰지 않고(산출물은 `sync` 경유), 봉인 수단을 바꾸면 `security-reviewer`
재심 대상이다. **봉인은 두 프로파일로 갈리고 합칠 수 없다**(인증 토큰이 config dir별로 분리된다).
유료 세션은 실제 config dir에 churn을 남기므로 "변경 0건"이 아니라 **실측 허용목록 기준**으로
판정한다 — 프로파일별 수단은 `docs/harness-facts.md`.

**읽기와 쓰기를 한 모듈에 섞지 않는다** — 쓰기는 사용자의 실제 환경을 바꾸므로 파괴적이다.

### 스키마의 척추 — 머신 종속 / 머신 독립

| 성격 | 엔티티 | 동기화 |
|---|---|---|
| 머신 **종속** | Machine · Installation(scope·enabled·토큰) · Project · Usage | 머신별로 따로 쌓임 |
| 머신 **독립** | Asset(툴 정체) · **Annotation(언제 쓰는가)** · Tag | 전 머신 공유 |

문제 1의 "어떤 상황에 어떤 툴"은 **Annotation**이고 머신이 바뀌어도 유지돼야 한다.
문제 3의 "이 로컬에 깔려 있나"는 **Installation**이고 머신마다 달라야 한다. **이 둘을 섞지 않는다.**

**스냅샷은 append-only로 쌓고 현재 상태·드리프트는 파생값으로 계산한다** — 기록 없이 유입된
툴은 현재 상태만 봐서는 못 찾고 두 시점을 diff해야 드러난다.

## 저장소 경계 — 3개는 서로 독립이다

| 저장소 | 공개 | 담는 것 |
|---|---|---|
| `claude-toolkit-console` (이곳) | **public** | 제품 코드만 |
| `../claude-toolkit-ops` | private | 사람이 쓰는 운영 이력 |
| 동기화 데이터 저장소 | **private** | `ctk`가 push/pull하는 스냅샷 |

**⚠️ 이곳은 public이므로 개인 환경 데이터를 코드·테스트·커밋에 넣지 않는다.**

- 금지: 실제 머신명 · 홈 경로 · 프로젝트 절대경로 · 설치된 툴 목록 · 개인 사용량/토큰 수치
- 테스트 픽스처는 **익명화된 합성 데이터**로 만든다
- **스냅샷 결과물을 커밋하지 않는다.** 동기화 저장소는 사용자 설정이지 제품 상수가 아니다

## 작업 워크플로우 — omc(oh-my-claudecode) 전용

**이 프로젝트의 개발 워크플로우는 omc 플러그인만 사용한다.** 플러그인 성능을 검증하려는 의도적
제약이다 — 다른 **워크플로우/프로세스** 플러그인(`superpowers`·`feature-dev` 등)과 병행하지
않는다(도메인 도구는 계속 쓴다).

**단계별 스폰 대상·금지 목록·세션 간 컨텍스트 규약은 `docs/workflow-assets.md`에 있다.** 시작 전에
그 단계의 omc 자산을 먼저 스폰한다(건너뛰지 않는다). `actuator` 변경에는 `security-reviewer` **필수**.

## 도메인 지식 — 자산 유형마다 다루는 법이 다르다

`actuator` 설계의 핵심이다. **유형을 하나로 추상화하려 하지 말 것.**
유형별 스코프 위치와 끄는 방법은 `docs/harness-facts.md`의 「자산 유형별 스코프·토글」에 있다.

**설치 스코프와 활성 여부는 다른 축이다.** `enabledPlugins`를 고쳐도 설치 스코프는 안 바뀐다 —
"전역에 두되 여기서만 켠다"는 활성 여부이고 스코프 이동은 `uninstall`+`install`이다. 뭉뚱그리지 않는다.

**실측 사실은 `docs/harness-facts.md`에 있다.** 하네스의 파일 구조·CLI 동작·함정이
거기 모여 있고, 새 사실을 알게 되면 그 문서에 추가한다(관측 방법과 파급을 함께 적는다).
**추측이나 도움말 인용만으로는 넣지 않는다.**

**"0건"은 범위와 축을 함께 의심한다.** **범위가 충분해도 축이 어긋나면 정확히 통과한다**(위생
게이트는 경로 리터럴을 찾는데 새는 것은 디렉터리 **이름**이었다). **어긋난 축은 과잉 차단도
만든다.** **한 판정 함수를 두 축이 쓰면 각 축에서 그 규칙이 여전히 옳은지 따로 묻는다** —
**무엇을 어떤 축으로** 봤는지 먼저 적는다.

## 안전 원칙

1. `actuator`의 모든 쓰기는 **백업 → 수정 → 검증 → (실패 시) 롤백** 순서를 지킨다.
2. 되돌릴 수 없는 조치(삭제)보다 **되돌릴 수 있는 조치(비활성·이동)를 먼저** 제안한다.
3. 조치 결과는 **실행 전/후 상태를 함께** 기록한다.
4. 사용자 환경을 바꾸는 코드는 `Agent(oh-my-claudecode:security-reviewer)`를 반드시 거친다.
5. **리뷰 지적은 항목이 아니라 범위로 닫는다.** **방어를 만든 것과 모든 경로에 배선한 것은 다르다**
   — 아홉 번 나왔고 아홉 번째는 그 누락을 고치며 **세 자리 중 두 자리만** 고쳤다. 머릿속 모델로
   훑지 말고 **호출 지점을 세어 전부 배선하고 타입으로 고정한다**(선택 필드는 누락을 통과시킨다).
6. **fail-closed 가드에는 복구 경로와 진단을 함께 만든다.** 빠져나갈 길이 없으면 사용자는 가드를
   우회하는 법부터 찾고, 그 순간 가드는 사라진다. 여덟 번 나왔다(사례 `docs/harness-facts.md`).
   **가드와 그 가드가 검사할 값을 함께 놓고 본다** — 복구 명령이 있어도 그것이 검사할 값(양성
   대조군)이 실환경에 없으면 통과할 수 없다.
7. **"없음"과 "실패"를 구분한다.** 이 코드베이스에서 반복해 나온 결함은 전부 같은 뿌리였다 —
   파싱 실패를 빈 결과로, 미실행을 "0건 일치"로, 중복 키를 last-write-wins로 삼키는 코드다.
   판정할 수 없으면 **에러를 던지거나 `unmeasured`로 남긴다.** 조용히 그럴듯한 값을 만들지 않는다.
   **표시를 갈랐으면 그 값을 만드는 계층도 갈렸는지 본다** — 상세 화면을 3분할한 다음 날 그 아래
   리졸버가 여전히 `boolean` 하나로 다섯 갈래를 뭉개고 있었다. 화면은 아래가 뭉갠 만큼만 정확하다.
8. **개별 값이 전부 정확해도 집계는 거짓 결론을 낸다.** 상위 5가 전부 0토큰이었고, 견적은
   **중앙값 × 건수**로 총액을 11~21% 낮게 말했다(합계는 평균 × 건수다). **모집단이 결론을
   지탱하는지 싣고, 집계식은 한 곳에만 둔다** — 표시가 곱하면 **화면 수만큼** 갈라진다.

## 검증은 무엇을 검증하는지 확인한다

통과했다는 사실이 동작한다는 뜻은 아니다. 이 프로젝트에서 실제로 어긋났던 조합:

- **테스트 통과 ≠ 실행됨** — vitest가 `main`을 우회해도, 문자열 UI가 `SyntaxError`로 깨져도
  통과했다. **문자열로 조립한 산출물은 최소 스텁 위에서 실행해 확인한다.** **실행 테스트가
  있다 ≠ 이 경로가 실행된다** — 고친 경로를 **직접** 태운다.
- **규칙 존재 ≠ 규칙이 막음** — lint 규칙은 위반 코드를 만들어 걸리는지, 컴파일 전용 테스트는
  `@ts-expect-error`를 떼어, CI 게이트는 **실제로 돈 기록**을 본다(5회가 `Setup Node`에서 죽어
  있었다). **실패가 아무것도 막지 않으면 신호가 아니다.**
- **안전이 두 조건의 맞물림에 달렸으면 그 결합을 테스트한다** — 각각만 검증돼 있던 적이 있다.
- **통과는 "검증됨"이 아니고, 실패는 "막힘"이 아니며, 관측 도구도 거짓말한다**(사례 `docs/`).
  **루프 관측과 단독 실행이 어긋나면 코드보다 측정을 먼저 의심한다.**
- **문서·계획서에 적은 명령은 한 번은 실행해 본다**(`pnpm release:seal`은 필수 플래그가 빠져
  그대로는 실패한다). **독립 대조가 같은 파서를 두 번 돌리면 순환이다.**
- **테스트가 코드를 단언하는지 환경을 단언하는지 본다. 부재를 단언하려면 부재를 주입한다** —
  픽스처가 결과를 지배하지 못하면 통과는 그 머신에 대한 진술이다(사례 `docs/`).
- **주장된 수정은 직접 주입해 확인한다** — 동어반복인지 가르는 방법이다. **픽스처는 실제 파서를
  통과시키고(`as T` 금지), 판정이 낼 수 있는 값 전체를 담는다**(대칭 표본에는 중앙값/평균 결함이
  안 드러난다). **파괴 실험에도 축이 있다** — 같은 축만 흔들다 신규 생성 축의 High를, 정상 해제
  경로만 흔들다 **신호 발화** 축의 과잉 삭제를 놓쳤다(주입해도 통과했다). **못 태우는 축이 있으면
  이음매를 넣어 태운다.** 파일시스템으로 못 만드는 입력(`..`·NUL)은 손으로 조립한다.
  **주입 전 `cp`로 백업한다** — `git checkout <파일>`은 그 세션의 작업을 지운다.
- **`pnpm test`는 타입을 보지 않는다** — 키 오타와 잘못 좁힌 타입이 전량 통과했고 `pnpm verify`가
  잡았다. **좁힌 타입은 축을 지운다**(`exitCode: number`가 "신호로 죽었다"(`null`)를 없앴다).
- **프롬프트 산출물(스킬·에이전트 지시)은 정적 게이트를 다 통과하고도 틀린다** — 결함 4건이
  lint·테스트를 통과했고 **띄워야** 보였다. 고쳤으면 **다시 띄워** 본다.
- **리뷰·재심 지적도 검증 대상이다.** 그대로 넣었더니 정상 동작이 위반이 된 적이 있다 —
  계층이 다르면 같은 신호의 의미도 다르다. 받되 **범위를 잰다**.
- **미측정은 통과가 아니다.** 재는 순간 위반인 항목이 있었다(AC-3.1: 89/60).
  **못 잰 항목은 릴리스 판정에서 "아직 실패일 수 있음"으로 센다.**
- **게이트는 만들 때 *동시에* `pnpm verify`에 등록한다.** 계획서에만 있고 표준 명령에 없으면
  한 번도 실행되지 않는다 — 한 세션에만 세 번이었다.
- **유료·외부 호출은 소요 시간으로 실행 여부를 검산한다** — 유료 5회가 4.47초에 끝난 것은 모델이
  한 번도 안 돌았다는 뜻이었다. **관측 출력은 첫 실행에서 다 받는다**(재실행 = 비용 2배).

## 커밋 규칙

- 이 저장소는 **제품 코드 전용**이다. 운영 기록은 `../claude-toolkit-ops`에 남긴다. 섞지 않는다.
- 커밋은 `Agent(oh-my-claudecode:git-master)`로 수행한다.

### 커밋은 승인 없이, PR·머지는 승인받고

| 행위 | 승인 |
|---|---|
| 작업 브랜치 생성 | 불요 |
| **작은 작업 단위 커밋** | **불요 — 판단해서 계속 진행한다** |
| 작업 브랜치 push | 불요 |
| **PR 생성 · 머지** | **필수 — 큰 작업이 끝날 때마다 사용자에게 묻는다** |
| `main`에 직접 커밋 | **금지** |

**작은 작업 단위**란 그 자체로 되돌릴 수 있고 검증이 통과한 변경이다 — 모듈 하나, 버그 하나,
리팩터 하나. 검증(타입체크·lint·테스트)이 통과하지 않은 상태는 커밋하지 않는다.
여러 관심사가 섞였으면 나눠서 커밋한다.

**큰 작업**이란 사용자가 결과를 확인하고 방향을 바꿀 수 있는 단위다 — 계획서의 Step 하나,
내부 게이트(G1·G2), 릴리스(R3). 여기 도달하면 **무엇이 끝났고 무엇이 남았는지 요약해
PR·머지 여부를 묻는다.** 묻지 않고 머지하지 않는다.

작업 중 발견한 사실(실측 결과·설계 변경 근거)은 커밋 메시지 본문에 남긴다.
"무엇을 바꿨다"보다 **"왜 그렇게 바꿔야 했는지"**가 나중에 필요한 정보다.

### PR을 열기 전에 회고한다

PR 생성 **직전에 항상** 그 세션의 작업을 회고해 작업 지침을 갱신하고 **같은 PR에 포함**한다.

- `Skill(claude-md-management:revise-claude-md)`로 수행한다.
- **`CLAUDE.md`는 200줄을 넘기지 않는다.** 넘으면 판단 기준만 남기고 참조 자료는
  `docs/`로 이관한다(실측 사실 → `docs/harness-facts.md`).
- 반복해서 드러난 **실패 패턴**을 우선 반영한다. 일회성 수정은 넣지 않는다.

## 상속

사용자 전역 `~/.claude/CLAUDE.md`의 규칙이 이 프로젝트에도 그대로 적용된다 —
**토큰 과다 소비 작업의 사전 고지·승인**, **백그라운드 대기의 데드라인과 직접 검수**.
**고지하는 숫자는 코드에서 확인한다** — `--max-budget-usd`를 "배치 총액"으로 고지했으나 CLI
경로에서는 **호출당** 상한이었다(나누는 로직은 웹 승인 화면에만 있다). **상한 ≠ 예상 지출**이다.
**긴 유료 실행 중에는 그 실행이 관측하는 대상을 건드리지 않는다** — 같은 세션에서 테스트·빌드를
돌리자 `gen`의 churn이 12%→100%, 자산당 1.26→3.8분이 됐다. **관측이 대상과 경쟁한다.**

## 현재 상태

v1 완료(요구사항 8/8, 심사 High/Critical 0건). **남은 것과 진척은 `ROADMAP.md`가 정본이다** —
여기서 중복하지 않는다. 상세 계획은 `.omc/plans/`.
