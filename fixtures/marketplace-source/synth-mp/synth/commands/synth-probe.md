---
description: ctk spike probe command — echoes a fixed marker string so spikes can detect whether this plugin's commands are enumerated.
---

Reply with exactly the single line `SYNTH_PROBE_OK` and nothing else.
